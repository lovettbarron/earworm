---
phase: 08-coverage-doc-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TEST-12]

must_haves:
  truths:
    - "Overall project line coverage exceeds 80%"
    - "ROADMAP.md phase checkboxes match actual completion state"
    - "ROADMAP.md progress table is accurate"
    - "REQUIREMENTS.md traceability table is accurate (TEST-12 marked complete)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase completion status"
      contains: "[x] **Phase 1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement traceability"
      contains: "[x] **TEST-12"
  key_links:
    - from: "go test ./... -coverprofile"
      to: ".planning/REQUIREMENTS.md"
      via: "Coverage measurement proves TEST-12 completion"
      pattern: "total:.*[89][0-9]\\.[0-9]%"
---

<objective>
Fix stale documentation artifacts (ROADMAP.md checkboxes and progress table, REQUIREMENTS.md traceability) and verify overall project test coverage exceeds the 80% threshold required by TEST-12.

Purpose: Clean milestone close -- documentation reflects reality, coverage gate passes.
Output: Updated ROADMAP.md and REQUIREMENTS.md, coverage verification proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-coverage-doc-cleanup/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md checkboxes and progress table to reflect actual completion</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state with stale checkboxes)
    - .planning/phases/08-coverage-doc-cleanup/08-RESEARCH.md (documents exactly which checkboxes are wrong, see "Documentation Artifacts to Fix" section)
  </read_first>
  <action>
Update `.planning/ROADMAP.md` with these specific changes:

**Phase checkboxes (lines ~15-21):**
Change the 6 main phase checkboxes from `[ ]` to `[x]` for Phases 1-6:
```
- [x] **Phase 1: Foundation & Configuration** - ...
- [x] **Phase 2: Local Library Scanning** - ...
- [x] **Phase 3: Audible Integration** - ...
- [x] **Phase 4: Download Pipeline** - ...
- [x] **Phase 5: File Organization** - ...
- [x] **Phase 6: Integrations & Polish** - ...
```
Leave Phase 7 and Phase 8 as-is (they may still be in progress when this runs -- check SUMMARY files).

**Plan checkboxes:**
- Phase 6: Change `- [ ] 06-02-PLAN.md` to `- [x] 06-02-PLAN.md` (it was completed)
- Phase 7: Change `- [ ] 07-01-PLAN.md` to `- [x] 07-01-PLAN.md` and `- [ ] 07-02-PLAN.md` to `- [x] 07-02-PLAN.md` (Phase 7 completed before Phase 8 started per dependency)

**Progress table (near bottom):**
Update to reflect actual state:
```
| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Foundation & Configuration | 3/3 | Complete | Yes |
| 2. Local Library Scanning | 2/2 | Complete | Yes |
| 3. Audible Integration | 3/3 | Complete | Yes |
| 4. Download Pipeline | 4/4 | Complete | Yes |
| 5. File Organization | 2/2 | Complete | Yes |
| 6. Integrations & Polish | 3/3 | Complete | Yes |
| 7. Fix Download→Organize Pipeline | 2/2 | Complete | Yes |
| 8. Test Coverage & Doc Cleanup | 0/3 | Executing | - |
```

**Phase 8 Plans section:**
Update `**Plans**: 0 plans` to `**Plans**: 3 plans` and add:
```
Plans:
- [ ] 08-01-PLAN.md — Test coverage for metadata, venv, audible, config, db, download packages
- [ ] 08-02-PLAN.md — Test coverage for cli package
- [ ] 08-03-PLAN.md — Documentation cleanup and coverage verification gate
```
  </action>
  <verify>
    <automated>cd /Users/albair/src/earworm && grep -c '\[x\] \*\*Phase' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "[x] \*\*Phase 1" .planning/ROADMAP.md returns match
    - grep "[x] \*\*Phase 2" .planning/ROADMAP.md returns match
    - grep "[x] \*\*Phase 3" .planning/ROADMAP.md returns match
    - grep "[x] \*\*Phase 4" .planning/ROADMAP.md returns match
    - grep "[x] \*\*Phase 5" .planning/ROADMAP.md returns match
    - grep "[x] \*\*Phase 6" .planning/ROADMAP.md returns match
    - grep "[x] 06-02-PLAN.md" .planning/ROADMAP.md returns match
    - grep "[x] 07-01-PLAN.md" .planning/ROADMAP.md returns match
    - grep "3/3 | Complete" .planning/ROADMAP.md returns at least 3 matches (phases 1,3,6)
    - grep "Plans: 3 plans" .planning/ROADMAP.md returns match (for Phase 8)
  </acceptance_criteria>
  <done>ROADMAP.md accurately reflects completion state for all phases and plans</done>
</task>

<task type="auto">
  <name>Task 2: Verify overall coverage gate and mark TEST-12 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state -- TEST-12 is the only unchecked requirement)
  </read_first>
  <action>
1. Run the coverage measurement:
```bash
go test ./... -coverprofile=coverage.out -count=1
go tool cover -func=coverage.out | grep "total:"
```

2. Extract the total percentage. If it is >= 80%, proceed. If below 80%, identify which packages are still below threshold and note them in the SUMMARY (do NOT block -- the gap should be small after Plans 01 and 02).

3. Run per-package coverage check:
```bash
go test ./... -cover -count=1
```
Verify each non-cmd package reports >= 80%.

4. Update `.planning/REQUIREMENTS.md`:
Change line:
```
- [ ] **TEST-12**: All packages maintain >80% line coverage; no phase ships without passing `go test ./...`
```
To:
```
- [x] **TEST-12**: All packages maintain >80% line coverage; no phase ships without passing `go test ./...`
```

5. Update traceability table -- change:
```
| TEST-12 | Phase 8 | Pending |
```
To:
```
| TEST-12 | Phase 8 | Complete |
```

6. Update the last-updated date line at the bottom to today's date.

7. Clean up coverage.out artifact:
```bash
rm -f coverage.out
```
  </action>
  <verify>
    <automated>cd /Users/albair/src/earworm && go test ./... -coverprofile=/tmp/earworm-final.out -count=1 && TOTAL=$(go tool cover -func=/tmp/earworm-final.out | grep "total:" | awk '{print $NF}' | tr -d '%') && echo "Total coverage: ${TOTAL}%" && rm -f /tmp/earworm-final.out</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 (all tests pass)
    - `go tool cover -func` total line shows >= 80.0%
    - grep "[x] \*\*TEST-12" .planning/REQUIREMENTS.md returns match
    - grep "TEST-12 | Phase 8 | Complete" .planning/REQUIREMENTS.md returns match
    - grep "Pending" .planning/REQUIREMENTS.md returns 0 matches
  </acceptance_criteria>
  <done>Overall project coverage >= 80%, TEST-12 marked complete, all 43 requirements in REQUIREMENTS.md are checked</done>
</task>

</tasks>

<verification>
```bash
# Full test suite passes
go test ./... -count=1

# Overall coverage gate
go test ./... -coverprofile=/tmp/cov.out -count=1 && go tool cover -func=/tmp/cov.out | grep "total:" && rm -f /tmp/cov.out

# Documentation accuracy
grep -c '\[x\]' .planning/REQUIREMENTS.md  # Should be 43
grep -c '\[ \]' .planning/REQUIREMENTS.md  # Should be 0
grep -c '\[x\] \*\*Phase' .planning/ROADMAP.md  # Should be >= 6
```
</verification>

<success_criteria>
- Overall line coverage >= 80% (excluding cmd/earworm which is 0% by convention)
- All 43 v1 requirements in REQUIREMENTS.md are marked [x]
- ROADMAP.md phase checkboxes and progress table are accurate
- `go test ./... -count=1` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-coverage-doc-cleanup/08-03-SUMMARY.md`
</output>
