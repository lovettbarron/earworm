---
phase: 01-foundation-configuration
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - README.md
  - .goreleaser.yaml
  - CLAUDE.md
autonomous: true
requirements:
  - CLI-04

must_haves:
  truths:
    - "README documents how to install Earworm from source"
    - "README documents audible-cli as a dependency and how to install it"
    - "README documents available commands (version, config init/show/set)"
    - "README documents configuration file location and available settings"
    - "GoReleaser config enables cross-platform builds with version injection"
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation"
      contains: "## Installation"
    - path: ".goreleaser.yaml"
      provides: "Build configuration for releases"
      contains: "ldflags"
  key_links:
    - from: ".goreleaser.yaml"
      to: "cmd/earworm/main.go"
      via: "ldflags inject version/commit/date"
      pattern: "main\\.version"
---

<objective>
Create comprehensive README documentation and GoReleaser build configuration. Update CLAUDE.md with corrected library versions discovered during research.

Purpose: Satisfy CLI-04 (README documents installation, setup, and commands). Establish the build pipeline for binary distribution. Correct version drift in CLAUDE.md so future phases use accurate dependency info.
Output: README.md with install/setup/usage docs, .goreleaser.yaml for builds, corrected CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-configuration/01-CONTEXT.md
@.planning/phases/01-foundation-configuration/01-RESEARCH.md
@.planning/phases/01-foundation-configuration/01-01-SUMMARY.md
@.planning/phases/01-foundation-configuration/01-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README with installation, setup, and usage documentation</name>
  <files>README.md</files>
  <read_first>
    CLAUDE.md (project description and constraints),
    .planning/phases/01-foundation-configuration/01-CONTEXT.md (D-06 for commands, D-04 for config path),
    internal/cli/root.go (actual command structure),
    internal/config/config.go (actual config keys and defaults)
  </read_first>
  <action>
Create `README.md` with the following sections and content:

**# Earworm**

One-line description: "A CLI-driven audiobook library manager for Audible."

Paragraph expanding: tracks local audiobook library (typically on NAS mount), downloads new books from Audible via audible-cli, organizes them in a Libation-compatible file structure, integrates with Audiobookshelf.

**## Features**

Bullet list of current capabilities (Phase 1):
- SQLite-backed library state tracking
- YAML configuration with sensible defaults
- `earworm version` -- display build information
- `earworm config init` -- create default configuration
- `earworm config show` -- display current settings
- `earworm config set <key> <value>` -- update a setting

Note: "More commands coming soon: scan, sync, download"

**## Prerequisites**

- Go 1.23 or later (for building from source)
- Python 3.9+ (required by audible-cli)
- [audible-cli](https://github.com/mkb79/audible-cli) -- install via `pip install audible-cli` or `uv pip install audible-cli`

**## Installation**

### From Source
```bash
go install github.com/lovettbarron/earworm/cmd/earworm@latest
```

### Build from Repository
```bash
git clone https://github.com/lovettbarron/earworm.git
cd earworm
go build -o earworm ./cmd/earworm
```

**## Quick Start**

```bash
# Initialize configuration
earworm config init

# Edit your config
earworm config set library_path /path/to/your/audiobooks

# Verify settings
earworm config show

# Check version
earworm version
```

**## Configuration**

Config file location: `~/.config/earworm/config.yaml`

Table of config keys with descriptions and defaults:

| Key | Description | Default |
|-----|-------------|---------|
| `library_path` | Path to audiobook library (can be NAS mount) | (empty) |
| `staging_path` | Temporary download directory | (empty, uses system temp) |
| `audible_cli_path` | Path to audible-cli binary | `audible` |
| `audiobookshelf.url` | Audiobookshelf server URL | (empty) |
| `audiobookshelf.token` | Audiobookshelf API token | (empty) |
| `audiobookshelf.library_id` | Audiobookshelf library ID | (empty) |
| `download.rate_limit_seconds` | Seconds between downloads | `5` |
| `download.max_retries` | Max retry attempts per book | `3` |
| `download.backoff_multiplier` | Exponential backoff multiplier | `2.0` |

**## Global Flags**

| Flag | Description |
|------|-------------|
| `--config <path>` | Use a custom config file path |
| `--quiet` / `-q` | Suppress non-essential output |

**## Setting Up audible-cli**

Brief instructions:
1. Install: `pip install audible-cli`
2. Authenticate: `audible quickstart` (follow prompts for Audible credentials)
3. Verify: `audible library list` should show your library

Note: "Earworm wraps audible-cli as a subprocess. Authentication is handled by audible-cli directly."

**## Data Storage**

- Configuration: `~/.config/earworm/config.yaml`
- Database: `~/.config/earworm/earworm.db` (SQLite, always local -- never on NAS)
- The database stores library state. It is safe to delete and will be recreated on next run.

**## License**

MIT License. See [LICENSE](LICENSE) for details.
  </action>
  <verify>
    <automated>cd /Users/albair/src/earworm && test -f README.md && grep -q "## Installation" README.md && grep -q "audible-cli" README.md && grep -q "## Configuration" README.md && grep -q "earworm config" README.md && echo "README OK"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "# Earworm"
    - README.md contains "## Installation"
    - README.md contains "## Prerequisites"
    - README.md contains "audible-cli"
    - README.md contains "pip install audible-cli"
    - README.md contains "## Configuration"
    - README.md contains "config.yaml"
    - README.md contains "library_path"
    - README.md contains "earworm config init"
    - README.md contains "earworm config show"
    - README.md contains "earworm config set"
    - README.md contains "earworm version"
    - README.md contains "--quiet"
    - README.md contains "## License"
  </acceptance_criteria>
  <done>README documents installation (from source, from repo), prerequisites (Go, Python, audible-cli), all current commands, configuration keys with defaults, global flags, and data storage locations. CLI-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Create GoReleaser config and correct CLAUDE.md versions</name>
  <files>.goreleaser.yaml, CLAUDE.md</files>
  <read_first>
    CLAUDE.md (current content with incorrect versions),
    .planning/phases/01-foundation-configuration/01-RESEARCH.md (correct versions: Cobra v1.10.2, Viper v1.21.0, Testify v1.11.1)
  </read_first>
  <action>
**Step 1: Create `.goreleaser.yaml`.**

Standard GoReleaser v2 config for Go CLI binary:

```yaml
version: 2

project_name: earworm

builds:
  - main: ./cmd/earworm
    binary: earworm
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
```

Key points:
- CGO_ENABLED=0 because we use modernc.org/sqlite (pure Go, no CGo)
- ldflags inject version, commit, date into cmd/earworm/main.go variables
- Builds for linux and darwin, both amd64 and arm64

**Step 2: Update CLAUDE.md with corrected library versions.**

Find and replace these version strings in CLAUDE.md (per RESEARCH.md findings):
- `v2.3.0` -> `v1.10.2` (Cobra -- v2 does not exist)
- `v1.11.0` -> `v1.21.0` (Viper)
- `v1.9+` -> `v1.11.1` (Testify)

Also add to the "## Conventions" section (currently says "not yet established"):
```
### Established Patterns
- **Project layout:** `cmd/earworm/` entry point, `internal/` for private packages
- **Database:** modernc.org/sqlite with driver name "sqlite" (NOT "sqlite3"), WAL mode enabled
- **Migrations:** Embedded SQL via `//go:embed migrations/*.sql`, sequential numbered files, schema_versions tracking table
- **Config:** Viper with YAML, config at ~/.config/earworm/config.yaml, DB at ~/.config/earworm/earworm.db
- **CLI:** Cobra commands in internal/cli/, one file per command, root has --quiet and --config flags
- **Testing:** testify/assert + testify/require, in-memory SQLite for DB tests, viper.Reset() between config tests
- **Error handling:** Cobra RunE pattern, wrap errors with fmt.Errorf("context: %w", err)
```

Also update the "## Architecture" section (currently says "not yet mapped"):
```
### Package Structure
- `cmd/earworm/` -- Binary entry point, version ldflags
- `internal/cli/` -- Cobra command definitions (root, version, config)
- `internal/config/` -- Viper setup, defaults, validation, path resolution
- `internal/db/` -- SQLite database, migrations, Book CRUD
```
  </action>
  <verify>
    <automated>cd /Users/albair/src/earworm && test -f .goreleaser.yaml && grep -q "main.version" .goreleaser.yaml && grep -q "v1.10.2" CLAUDE.md && grep -q "v1.21.0" CLAUDE.md && echo "Files OK"</automated>
  </verify>
  <acceptance_criteria>
    - .goreleaser.yaml contains `main: ./cmd/earworm`
    - .goreleaser.yaml contains `CGO_ENABLED=0`
    - .goreleaser.yaml contains `main.version`
    - .goreleaser.yaml contains `main.commit`
    - .goreleaser.yaml contains `main.date`
    - .goreleaser.yaml contains `linux` and `darwin`
    - .goreleaser.yaml contains `amd64` and `arm64`
    - CLAUDE.md contains `v1.10.2` (corrected Cobra version)
    - CLAUDE.md contains `v1.21.0` (corrected Viper version)
    - CLAUDE.md does NOT contain `v2.3.0` (removed incorrect Cobra version)
    - CLAUDE.md "Conventions" section contains "Established Patterns"
    - CLAUDE.md "Architecture" section contains "Package Structure"
  </acceptance_criteria>
  <done>GoReleaser configured for cross-platform builds with version injection. CLAUDE.md corrected with accurate library versions and populated with established conventions and architecture from Phase 1 implementation.</done>
</task>

</tasks>

<verification>
- README.md exists with all required sections
- .goreleaser.yaml exists with correct build config
- CLAUDE.md has corrected versions (no v2.3.0, no v1.11.0)
- Full test suite still passes: `go test -v -race -count=1 ./...`
</verification>

<success_criteria>
- README documents installation, audible-cli setup, all commands, configuration, and data storage
- GoReleaser config builds for linux/darwin amd64/arm64 with ldflags
- CLAUDE.md versions corrected (Cobra v1.10.2, Viper v1.21.0, Testify v1.11.1)
- CLAUDE.md conventions and architecture sections populated
- CLI-04 satisfied: README documents installation, setup, and all commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-configuration/01-03-SUMMARY.md`
</output>
